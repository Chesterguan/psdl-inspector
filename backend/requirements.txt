fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pyyaml>=6.0
httpx>=0.25.0
lark>=1.1.0  # Required by psdl-lang for expression parsing
psdl-lang>=0.4.0  # Always track latest psdl-lang
python-docx>=0.8.11  # Word document generation for IRB export
pyarrow>=15,<17
meds>=0.3,<0.4
# psdl_ai_gen is installed editable from ./psdl_ai_gen during development
# (pip install -e ./psdl_ai_gen).
