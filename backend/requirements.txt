fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pyyaml>=6.0
httpx>=0.25.0
lark>=1.1.0  # Required by psdl-lang for expression parsing
psdl-lang>=0.4.0  # Always track latest psdl-lang
python-docx>=0.8.11  # Word document generation for IRB export
pyarrow>=15,<17
meds>=0.3,<0.4
# psdl_vocab is installed editable from ./psdl_vocab during development
# (pip install -e ./psdl_vocab). Bundled OMOP vocab ships with the package.
# psdl_ai_gen is installed editable from ./psdl_ai_gen during development
# (pip install -e ./psdl_ai_gen).
# psdl_vocab_search is installed editable from ./psdl_vocab_search during development
# (pip install -e ./psdl_vocab_search). ML embedders are optional extras: .[ml]
# psdl_anchoring is installed editable from ./psdl_anchoring during development
# (pip install -e ./psdl_anchoring).
# psdl_observatory is installed editable from ./psdl_observatory during development
# (pip install -e ./psdl_observatory). EDW inventory scanner (O1).
# preflight (offline SQL Preflight core) is an OPTIONAL add-on — NOT installed by
# default. The /api/preflight router degrades to 503 when it is absent, so the rest
# of Inspector runs fine without it. Enable the feature with:
#   pip install -r requirements-preflight.txt
